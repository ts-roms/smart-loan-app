// Public API of the audit feature. The drawer trigger is rendered in the
// staff navbar (DashboardShell) and gates itself on `admin.audit_log`.
export { AuditLogTrigger } from './components/AuditLogDrawer';
